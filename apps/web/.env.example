PAYLOAD_URL=http://localhost:3000
# Same value, exposed to client-side scripts (form submissions) — must be PUBLIC_-prefixed for Astro/Vite to inline it into browser bundles.
PUBLIC_PAYLOAD_URL=http://localhost:3000
